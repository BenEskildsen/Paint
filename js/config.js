const config = {
  tools: [
    'PEN', 'ERASER', 'BUCKET', 'SELECT', 'PIPETTE',
  ],
  edits: [
    'UNDO', 'REDO', 'ERASE ALL',
  ],
  colors: [
    'red', 'green', 'blue', 'black', 'white', 'gray', 'steelblue',
  ],

  // dimensions
  initialWidth: 500,
  initialHeight: 500,
  toolBarWidth: 75,
  colorBarHeight: 75,
};

module.exports = {
  config,
};
